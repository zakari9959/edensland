/*import React from 'react';
import books from '../../data/books.json';



interface FlipPageProps {
  bookId: number;
  pageNb: number;
}

const FlipPage: React.FC<FlipPageProps> = ({ bookId , pageNb }) => {
  
  const numberOfPages: number = currentBook.text ? Math.ceil(currentBook.text.length);
    return (
      <div>
        <p>{currentBook.text[]}</p>
      </div>
    );
  };

export default FlipPage;*/